/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Context, Pda, PublicKey, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { transactionBuilder } from "@metaplex-foundation/umi";
import { mapSerializer, struct, u8 } from "@metaplex-foundation/umi/serializers";

import { findAssertionV1Pda } from "../accounts";
import { expectPublicKey, getAccountMetasAndSigners } from "../shared";

// Accounts.
export type ResolveAssertionV1InstructionAccounts = {
  /** Config */
  config: PublicKey | Pda;
  /** Request */
  request: PublicKey | Pda;
  /** Assertion */
  assertion?: PublicKey | Pda;
};

// Data.
export type ResolveAssertionV1InstructionData = { discriminator: number };

export type ResolveAssertionV1InstructionDataArgs = {};

export function getResolveAssertionV1InstructionDataSerializer(): Serializer<
  ResolveAssertionV1InstructionDataArgs,
  ResolveAssertionV1InstructionData
> {
  return mapSerializer<
    ResolveAssertionV1InstructionDataArgs,
    any,
    ResolveAssertionV1InstructionData
  >(
    struct<ResolveAssertionV1InstructionData>([["discriminator", u8()]], {
      description: "ResolveAssertionV1InstructionData",
    }),
    (value) => ({ ...value, discriminator: 8 }),
  );
}

// Instruction.
export function resolveAssertionV1(
  context: Pick<Context, "eddsa" | "programs">,
  input: ResolveAssertionV1InstructionAccounts,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "optimisticOracle",
    "DVM2j1a1AJ9hZuEXyMxA5vusKgMR2FcKJyCf3QE5R2ge",
  );

  // Accounts.
  const resolvedAccounts = {
    config: {
      index: 0,
      isWritable: false as boolean,
      value: input.config ?? null,
    },
    request: {
      index: 1,
      isWritable: true as boolean,
      value: input.request ?? null,
    },
    assertion: {
      index: 2,
      isWritable: false as boolean,
      value: input.assertion ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.assertion.value) {
    resolvedAccounts.assertion.value = findAssertionV1Pda(context, {
      request: expectPublicKey(resolvedAccounts.request.value),
    });
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getResolveAssertionV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
